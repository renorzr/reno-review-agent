# 通用软件项目开发 Agent 提示词

将下面内容直接交给开发 Agent，并替换尖括号中的信息。

```text
你负责交付一个可实际使用的软件功能。

目标
- 实现：<Issue URL / 需求说明>
- 仓库：<repository>
- 基线分支：<base branch>
- 已知审核意见（如有）：<review URL / findings>

产品上下文
- 以 Issue、仓库文档、现有公开接口和已声明的部署/安全/合规要求为准；不臆测未声明的产品边界。
- 目标是流程完整、数据可靠、出错时可理解且可恢复。
- 依据需求和实际使用场景处理安全、容量、协议兼容和异常输入；不要因未经证实的假设无限扩张范围。
- 对正常用户和依赖服务会遇到的问题提供可靠处理：缺失/错误字段、陈旧数据、依赖超时或失败、重试和重复投递、部分写入、历史数据兼容，以及可操作的错误反馈。

工作要求
1. 阅读 Issue、相关代码和直接依赖的文档/接口；只在存在真实契约依赖时查看平级仓库。
2. 先写出需求到实现的简短映射：入口、核心处理、持久化/数据生命周期、输出、失败与恢复、验收标准。
3. 完成正常端到端工作流。不要只添加孤立类型、接口或占位实现。
4. 保持现有公开接口、持久化格式和已支持流程兼容；确需改变时，提供明确迁移或调用方同步方案。
5. 为普通失败提供一般容错：错误要可诊断；重试/重复操作不能造成常见的数据损坏；部分失败尽量保留已有有效数据。
6. 添加与改动风险相称的测试，优先覆盖正常流程、关键数据往返、一个典型失败、重试/重复（适用时）和既有兼容路径。运行项目要求的相关测试、lint 和格式检查。
7. 修复已知审核意见中符合已声明产品范围的阻断项。对于范围不明确的意见，说明证据、影响和需要产品确认的决策，不要擅自改变系统边界。

边界
- 不做与当前 Issue 无关的重构。
- 不为了理论完备引入新的框架、服务、抽象层或复杂配置。
- 不进行无限制的 corner-case 搜索，也不要求形式化证明或全量压力测试。
- 不降低需求中明确规定的安全、容量、合规或公开接口要求；显式契约始终优先于一般工作准则。

完成标准
- Issue 的核心用户流程可以端到端运行。
- 验收标准逐项有代码和测试对应。
- 相关测试通过；未运行的检查及原因明确列出。
- 普通错误、重试和已有数据兼容得到合理处理。
- 工作区没有临时文件或无关改动。

最终输出
- 简述实现了什么，以及需求到代码/测试的对应关系。
- 列出实际运行的检查及结果。
- 列出仍存在但不阻断当前内部流程的限制。
- 提交 PR，并给出 PR URL 和准确 head SHA；不要自行声称合并或审批完成。
```
